#!/bin/sh

if [ ! -f "/home/ebrignon/.config/chezmoi/key.txt" ]; then
    mkdir -p "/home/ebrignon/.config/chezmoi"
    chezmoi age decrypt --output "/home/ebrignon/.config/chezmoi/key.txt" --passphrase "{{ .chezmoi.sourceDir }}/key.txt.age"
    chmod 600 "/home/ebrignon/.config/chezmoi/key.txt"
fi
