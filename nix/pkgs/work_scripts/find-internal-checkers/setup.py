from setuptools import setup

package = 'find-internal-checker'
version = '0.1'

setup(name=package,
      version=version,
      description="Finds internal checkers in the current sandbox",
      url='url',
      packages=['find-internal-checkers'],
      )
